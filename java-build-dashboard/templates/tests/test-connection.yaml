apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "java-build-dashboard.fullname" . }}-test-connection"
  labels:
    {{- include "java-build-dashboard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "java-build-dashboard.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
